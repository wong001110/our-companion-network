# Future asset contract

Asset packs will use immutable manifests with SHA-256 hashes and direct, short-lived R2 upload/download authorization. The server stores metadata, not PNG binaries. S1 implements no asset upload, download, or caching.
